# Backend environment variables
# Copy this file to backend/.env and fill in values for local development.
# Never commit backend/.env (covered by root .gitignore).

# HTTP listen port (defaults to 3001 if unset)
PORT=3001

# Node environment
NODE_ENV=development

# Postgres connection string. In Docker compose this is composed
# automatically via the api service's environment block. For local dev
# point at a Postgres instance you control (e.g. a docker-run postgres
# container, or a Homebrew install of postgresql@16). Required at api
# startup — runMigrations() will refuse to start without it.
DATABASE_URL=postgres://ebay:dev@localhost:5432/ebay

# eBay OAuth ----------------------------------------------------------------
# Sandbox until M3 (per BUILD_PLAN.md "Sandbox-only until M3").
# Set EBAY_ENV=production once we go live.
EBAY_ENV=sandbox

# App keys from developer.ebay.com → My Account → Application Keys.
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=

# RuName (eBay Redirect URL alias). Created in the developer console at
# User Tokens → "Get a Token from eBay via Your Application" → Add eBay
# Redirect URL. Different RuName per environment.
EBAY_RUNAME_SANDBOX=
EBAY_RUNAME_PRODUCTION=

# Symmetric key (32 random bytes, base64-encoded) used to seal the eBay
# refresh token at rest. Stored in ebay_auth.refresh_token_sealed.
# Generate with:
#   openssl rand -base64 32 | tr -d '\n'
# Rotating this key would orphan all stored refresh tokens — re-OAuth
# is required for every connected account after a rotation.
EBAY_REFRESH_TOKEN_SEAL_KEY=

# eBay marketplace account deletion notifications (compliance endpoint).
# Both values must be shared with eBay when subscribing to account-deletion
# notifications in the developer console — they are required to enable the
# eBay production keyset. The verification token is a 32–80 character
# string of [A-Za-z0-9_-] that you choose; the endpoint URL is the public
# HTTPS URL where the GET/POST handlers are reachable.
EBAY_ACCOUNT_DELETION_VERIFICATION_TOKEN=
EBAY_ACCOUNT_DELETION_ENDPOINT_URL=https://ebay.rycsprojects.com/api/ebay/account-deletion
