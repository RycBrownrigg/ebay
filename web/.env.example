# Web environment variables
# Copy this file to web/.env.local and fill in values for local development.
# Never commit web/.env* (covered by root .gitignore).

# Public-facing variables prefixed NEXT_PUBLIC_* are exposed to the browser.
# Add them here as the app grows; none required for the M0 skeleton.
